// Dashboard overview page — shows the user's goal tracking summary and conversation history
// TODO: replace with full dashboard when UI layer is built

export default function DashboardPage() {
  return (
    <main>
      <h1>Dashboard</h1>
      <p>儀表板建置中</p>
    </main>
  );
}
